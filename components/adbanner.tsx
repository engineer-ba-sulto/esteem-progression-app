import { Text, View } from "react-native";

export default function AdBanner() {
  return (
    <View className="w-full bg-blue-100 flex items-center justify-center h-16">
      <Text className="text-gray-500 font-semibold text-center">
        広告バナー
      </Text>
    </View>
  );
}
